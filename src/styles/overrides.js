let TextFieldOverride = {
    width: '60vw'
}

let formHelperOverride = {
    textAlign: 'center',
    fontSize: 20
}

let costOverride = {
    border: '2px solid #fff',
    width: '60vw',
    padding: '10px 0 10px 0',
    margin: '40px auto 0 auto',
    borderRadius: '5px',
    fontWeight: 'bold'
}

let ButtonOverride = {
    width: '60vw', 
    backgroundColor: '#ac162c',
    color: '#fff', 
    '&:hover': {
        backgroundColor: '#811121',
    }
}

let MenuItemOverride = {
    whiteSpace: 'normal'
}

let SelectOverride = {
    width: '60vw'
}

export {costOverride, formHelperOverride, TextFieldOverride, ButtonOverride, MenuItemOverride, SelectOverride }